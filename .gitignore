build/
models/
fixtures/*.raw
fixtures/*.png
fixtures/*.mkv
sanity/saxpy
*.engine
build-win-release/
build-win-trt11*/

# Linux Vulkan backend build artifacts
/build/
/build-vk/
libaji_rocm.so
aji_rocm_compile

# hipRTC JIT cache (per-machine, per-GPU; created at runtime; never commit)
*.co
cache/

# any build-*/ tree (build-sync, build-vk, ...)
/build-*/
